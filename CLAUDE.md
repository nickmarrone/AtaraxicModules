# AtaraxicModules

The `ataraxic_dsp` library API is documented in `lib/ataraxic_dsp/API.md` — consult it for explanations of existing components, and update it whenever you add or change anything in the library's public API.
